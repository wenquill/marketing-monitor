PORT=3001
NODE_ENV=production
DATA_DIR=/app/data
SCREENSHOTS_DIR=/app/screenshots
# How often to take screenshots (minutes)
CHECK_INTERVAL_MINUTES=30
# Allowed CORS origin (your EC2 public IP or domain, e.g. http://1.2.3.4)
# Leave unset to allow all origins in production
CORS_ORIGIN=
